<turbo-stream action="replace" target="authorization_{{profileid}}_{{appsubfunctionid}}">
    <template>
        <i id="authorization_{{profileid}}_{{appsubfunctionid}}" class="bi bi-{{icon}} check-link-{{level}}"></i>
    </template>
</turbo-stream>

